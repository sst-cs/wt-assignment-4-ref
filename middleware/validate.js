export const validatePost = (req, res, next) => {
    // TODO: Validate post body (title, content)
    // If missing -> 400 { error: 'Missing required fields' }
    // else next()
};

export const validateComment = (req, res, next) => {
    // TODO: Validate comment body (content)
    // If missing -> 400 { error: 'Missing required fields' }
    // else next()
};
